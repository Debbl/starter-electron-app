{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "outputs": ["dist", ".next", "out"],
      "dependsOn": ["^build"]
    },
    "typecheck": {},
    "lint": {},
    "dev": {
      "persistent": true,
      "cache": false
    }
  }
}
